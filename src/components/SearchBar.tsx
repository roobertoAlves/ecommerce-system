import { Search } from "lucide-react";

const SearchBar = () => {
  return (
    <div>
      <Search className="w-5 h-5 text-text-muted hover:text-primary hoverEffect" />
    </div>
  );
};

export default SearchBar;
